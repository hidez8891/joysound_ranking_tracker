<!DOCTYPE html>
<html lang="ja">
  <head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Joysound Ranking Watcher</title>
  <%= csrf_meta_tags %>

  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->

  <%= stylesheet_link_tag    "application", :media => "all" %>
</head>
<body>
  <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <%= link_to 'Ranking Tracker', '/', :class => 'brand' %>
        <%= link_to 'Joysound', JOYSOUND_URL, :class => 'brand joysound' %>
        <div class="nav-collapse">
          <ul class="nav">
            <% if defined?(@current_artist) %>
              <li <%= 'class=active' unless defined?(@current_song) %>>
              <%= link_to @current_artist.name, @current_artist %>
              </li>
            <% end %>
            <% if defined?(@current_song) %>
              <li <%= 'class=active' unless false %>>
              <%= link_to @current_song.title, @current_song %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
  <header>
    <div class="container">
      <%= yield :head %>
    </div>
  </header>
  <div class="container">
    <div class="content">
      <div class="row">
        <%= yield %>
      </div>
    </div>
  </div>
  <%= javascript_include_tag "application" %>
  <%= yield :extend_javascript %>
</body>
</html>
